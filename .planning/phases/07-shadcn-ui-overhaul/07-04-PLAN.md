---
phase: 7
plan: 04
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - frontend/src/pages/Dashboard/Dashboard.jsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Dashboard uses shadcn Card grid for property listing"
    - "Navigation uses shadcn Tabs or Breadcrumb"
    - "No custom CSS classes"
    - "Build passes"
---

<objective>
Convert Dashboard to use shadcn components.

Purpose: Style property listing with consistent cards.
Output: Dashboard with shadcn styling.
</objective>

<execution_context>
@/home/user/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/user/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<tasks>
<task type="auto">
  <name>Task 1: Convert Dashboard</name>
  <files>
    frontend/src/pages/Dashboard/Dashboard.jsx
  </files>
  <read_first>
    frontend/src/pages/Dashboard/Dashboard.jsx
  </read_first>
  <action>
Replace custom HTML with shadcn components:
- Property cards → shadcn <Card> with CardHeader/CardContent/CardFooter
- Property grid → Tailwind grid-cols-* with gap-*
- Any navigation → shadcn <Tabs> or <Breadcrumb>
- Buttons → shadcn <Button>
- Status indicators → shadcn <Badge>
  </action>
  <acceptance_criteria>
    - Dashboard uses Card, Button, Badge, Tabs from shadcn
    - Property cards are consistent
    - Layout is responsive
  </acceptance_criteria>
</task>
</tasks>
