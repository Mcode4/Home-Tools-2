---
phase: 7
plan: 03
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - frontend/src/pages/LoginPage/LoginPage.jsx
  - frontend/src/pages/SignupPage/SignupPage.jsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Login and Signup pages use shadcn Card/Input/Button/FieldGroup"
    - ".session-background replaced with Tailwind flex centering"
    - ".session-form replaced with shadcn Card"
    - ".auth-buttons replaced with shadcn Button"
    - "Form validation uses shadcn FieldGroup/Field pattern"
    - "Build passes"
---

<objective>
Convert Login and Signup pages to use shadcn components.

Purpose: Style auth pages with consistent form patterns.
Output: Login + Signup with shadcn styling.
</objective>

<execution_context>
@/home/user/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/user/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<tasks>
<task type="auto">
  <name>Task 1: Convert LoginPage</name>
  <files>
    frontend/src/pages/LoginPage/LoginPage.jsx
  </files>
  <read_first>
    frontend/src/pages/LoginPage/LoginPage.jsx
    frontend/src/index.css
  </read_first>
  <action>
Replace custom HTML/styling:
- Replace .session-background with Tailwind: "flex items-center justify-center min-h-screen bg-background"
- Replace .session-form with shadcn <Card> + <CardHeader> + <CardTitle> + <CardContent> + <CardFooter>
- Replace custom <input> with shadcn <Input>
- Replace .auth-buttons with shadcn <Button variant="default" className="w-full">
- Add <Label> for form fields
- Use shadcn form validation pattern
- Remove unused CSS imports
  </action>
  <acceptance_criteria>
    - LoginPage uses Card, Input, Label, Button from shadcn
    - Form has proper structure
    - Error handling preserved
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Convert SignupPage</name>
  <files>
    frontend/src/pages/SignupPage/SignupPage.jsx
  </files>
  <read_first>
    frontend/src/pages/SignupPage/SignupPage.jsx
  </read_first>
  <action>
Apply same pattern as LoginPage: replace session-form with shadcn Card/Input/Button.
  </action>
  <acceptance_criteria>
    - SignupPage uses same shadcn components
    - Form fields match existing fields
  </acceptance_criteria>
</task>
</tasks>
