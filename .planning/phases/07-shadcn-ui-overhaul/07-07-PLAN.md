---
phase: 7
plan: 07
type: execute
wave: 4
depends_on: ["01"]
files_modified:
  - frontend/src/pages/RenderPage/RenderPage.jsx
  - frontend/src/pages/RenderPage/RenderPage.css
  - frontend/src/pages/RenderPage/AssetsPanel/AssetsPanel.jsx
  - frontend/src/pages/RenderPage/PropertiesPanel/PropertiesPanel.jsx
  - frontend/src/pages/RenderPage/StageBar/StageBar.jsx
  - frontend/src/pages/RenderPage/Toolbar/Toolbar.jsx
  - frontend/src/pages/RenderPage/MapToggle/MapToggle.jsx
  - frontend/src/pages/RenderPage/MapToggle/MapToggle.css
  - frontend/src/pages/RenderPage/AssetsPanel/ExportsTab.jsx
  - frontend/src/pages/RenderPage/AssetsPanel/BuildToolsTab.jsx
autonomous: false
requirements: []
must_haves:
  truths:
    - "Left menu uses shadcn Sheet with side=left"
    - "Right panel uses shadcn Sheet with side=right"
    - "StageBar uses shadcn Button group"
    - "MapToggle uses shadcn Select"
    - "Toolbar uses shadcn Button with ghost variant"
    - "Old CSS files removed after migration"
    - "Build passes after each sub-task"
---

<objective>
Convert RenderPage and all sub-panels to use shadcn components.

Purpose: Style the largest page in the application with consistent shadcn components.
Output: RenderPage with shadcn styling across all panels.
</objective>

<execution_context>
@/home/user/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/user/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<tasks>
<task type="auto">
  <name>Task 1: Convert StageBar</name>
  <files>
    frontend/src/pages/RenderPage/StageBar/StageBar.jsx
  </files>
  <read_first>
    frontend/src/pages/RenderPage/StageBar/StageBar.jsx
  </read_first>
  <action>
Replace custom stage buttons with shadcn:
- Stage buttons → <Tabs> with TabsList + TabsTrigger or <Button> group
- Active stage → shadcn Button variant="default"
- Inactive → Button variant="outline"
- Disabled → Button variant="outline" with disabled prop
- Use Tailwind classes for layout
  </action>
  <acceptance_criteria>
    - StageBar uses shadcn Button or Tabs
    - Active/inactive/disabled states work
    - Stage switching preserved
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Convert MapToggle</name>
  <files>
    frontend/src/pages/RenderPage/MapToggle/MapToggle.jsx
  </files>
  <read_first>
    frontend/src/pages/RenderPage/MapToggle/MapToggle.jsx
    frontend/src/pages/RenderPage/MapToggle/MapToggle.css
  </read_first>
  <action>
Replace custom dropdown with shadcn <Select>:
- Map layer options → <Select> with SelectTrigger/SelectContent/SelectItem
- View mode options → same Select pattern
- Remove MapToggle.css import
  </action>
  <acceptance_criteria>
    - MapToggle uses shadcn Select
    - MapToggle.css removed
    - Both map layers and view modes work
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Convert AssetsPanel (left menu)</name>
  <files>
    frontend/src/pages/RenderPage/AssetsPanel/AssetsPanel.jsx
  </files>
  <read_first>
    frontend/src/pages/RenderPage/AssetsPanel/AssetsPanel.jsx
  </read_first>
  <action>
Replace custom left panel with shadcn <Sheet side="left">:
- Menu item list → shadcn <Button variant="ghost"> in a flex col
- Active menu item → Button variant="secondary"
- Tab content panels already use shadcn components from sub-tabs
- Menu spacing and layout via Tailwind
  </action>
  <acceptance_criteria>
    - Left panel uses shadcn Sheet or ScrollArea
    - Menu items use shadcn Button
    - Active state has visual indicator
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Convert PropertiesPanel (right panel)</name>
  <files>
    frontend/src/pages/RenderPage/PropertiesPanel/PropertiesPanel.jsx
  </files>
  <read_first>
    frontend/src/pages/RenderPage/PropertiesPanel/PropertiesPanel.jsx
  </read_first>
  <action>
Replace custom right panel with shadcn <Sheet side="right">:
- Section headers → shadcn h4 with text-sm font-medium
- Tree nodes → custom but with Tailwind classes: flex, items-center, gap-*, p-*, hover:bg-muted
- Property inputs → shadcn <Input> with <Label>
- Buttons within properties → shadcn <Button variant="outline" size="sm">
  </action>
  <acceptance_criteria>
    - Right panel uses shadcn Sheet or ScrollArea
    - Tree node items use Tailwind utility classes
    - Property inputs use shadcn Input/Label
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 5: Convert Toolbar and canvas layer</name>
  <files>
    frontend/src/pages/RenderPage/Toolbar/Toolbar.jsx
    frontend/src/pages/RenderPage/RenderPage.jsx
  </files>
  <read_first>
    frontend/src/pages/RenderPage/Toolbar/Toolbar.jsx
  </read_first>
  <action>
Replace toolbar buttons with shadcn <Button variant="ghost" size="sm">:
- Each tool button → Button with icon + label
- Active tool → Button variant="secondary"
- Apply same pattern to canvas floating toolbar in RenderPage.jsx
- Use Tailwind classes for toolbar positioning
  </action>
  <acceptance_criteria>
    - Toolbar uses shadcn Button
    - Active/inactive states work
    - All tools function correctly
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 6: Remove RenderPage.css and finalize</name>
  <files>
    frontend/src/pages/RenderPage/RenderPage.css
    frontend/src/pages/RenderPage/RenderPage.jsx
  </files>
  <read_first>
    frontend/src/pages/RenderPage/RenderPage.css
  </read_first>
  <action>
After all sub-panels are converted:
1. Check RenderPage.css for any remaining used selectors
2. Move any still-needed styles to Tailwind classes
3. Remove RenderPage.css import from RenderPage.jsx
4. Run build and fix any layout issues
  </action>
  <acceptance_criteria>
    - RenderPage.css removed
    - No broken layouts
    - Build passes
    - All 31 tests pass
  </acceptance_criteria>
</task>
</tasks>
