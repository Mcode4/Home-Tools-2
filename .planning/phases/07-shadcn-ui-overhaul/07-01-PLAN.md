---
phase: 7
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/index.css
  - frontend/src/lib/utils.ts
  - frontend/components.json
  - frontend/tailwind.config.js
  - frontend/postcss.config.js
  - frontend/src/components/ui/
autonomous: true
requirements: []
must_haves:
  truths:
    - "shadcn init completes with mira preset"
    - "Tailwind CSS is configured and working"
    - "src/lib/utils.ts has cn() utility"
    - "Core shadcn components installed: Button, Card, Input, Label, Select, Badge, Sheet, Dialog"
    - "Dark/light theme works via Tailwind class strategy"
    - "Existing three theme classes (blueprint) preserved as custom classes"
    - "Build passes after init"
---

<objective>
Initialize shadcn/ui with mira preset, set up Tailwind, migrate global index.css, and install core components.

Purpose: Foundation for complete styling overhaul.
Output: shadcn working with Tailwind + core components.
</objective>

<execution_context>
@/home/user/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/user/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<tasks>
<task type="auto">
  <name>Task 1: Backup and run shadcn init</name>
  <files>
    frontend/src/index.css
  </files>
  <read_first>
    frontend/src/index.css
    frontend/package.json
  </read_first>
  <action>
1. Backup current src/index.css to src/index.css.backup
2. Run: cd frontend && npx shadcn@latest init --preset mira --defaults
3. This installs Tailwind, creates postcss.config.js, tailwind.config.js, components.json, lib/utils.ts
4. This also modifies src/index.css with Tailwind directives and shadcn CSS variables
  </action>
  <acceptance_criteria>
    - components.json exists in frontend/
    - tailwind.config.js exists
    - postcss.config.js exists
    - src/lib/utils.ts exists with cn() function
    - src/index.css contains @tailwind directives
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Restore theme classes in index.css</name>
  <files>
    frontend/src/index.css
  </files>
  <read_first>
    frontend/src/index.css
    frontend/src/index.css.backup
  </read_first>
  <action>
After shadcn init, restore the three theme classes (.theme-light, .theme-dark, .theme-blueprint) from backup into the new index.css.
Map old CSS custom properties to shadcn semantic CSS variables:
  - --bg-main → theme bg colors
  - --bg-panel → card colors
  - --accent → primary colors
  - --text-main → foreground colors
  - --text-dim → muted-foreground colors
  - --border → border colors
  - --danger → destructive colors
  - --radius-* → --radius
Preserve the .theme-blueprint class with custom cyan accent (#00e5ff).
  </action>
  <acceptance_criteria>
    - Three theme classes work with shadcn dark/light strategy
    - Custom blue print theme class preserved
    - No duplicate CSS variables
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Install core shadcn components</name>
  <files>
    frontend/package.json
  </files>
  <read_first>
    frontend/package.json
  </read_first>
  <action>
Run: cd frontend && npx shadcn@latest add button card input label select badge sheet dialog separator scroll-area
This installs the base components needed across all pages.
  </action>
  <acceptance_criteria>
    - src/components/ui/button.tsx exists
    - src/components/ui/card.tsx exists
    - src/components/ui/input.tsx exists
    - src/components/ui/label.tsx exists
    - src/components/ui/select.tsx exists
    - src/components/ui/badge.tsx exists
    - src/components/ui/sheet.tsx exists
    - src/components/ui/dialog.tsx exists
    - src/components/ui/separator.tsx exists
    - src/components/ui/scroll-area.tsx exists
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Verify build passes</name>
  <files>
    frontend/package.json
  </files>
  <read_first>
    frontend/package.json
  </read_first>
  <action>
Run: cd frontend && npm run build
Fix any build errors if they appear.
  </action>
  <acceptance_criteria>
    - npm run build exits 0
    - No TypeScript or compilation errors
  </acceptance_criteria>
</task>
</tasks>
