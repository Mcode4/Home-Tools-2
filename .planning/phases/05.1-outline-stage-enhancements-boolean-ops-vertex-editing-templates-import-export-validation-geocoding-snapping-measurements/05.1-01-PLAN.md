---
phase: 05.1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/RenderPage/AssetsPanel/ShapesTab.jsx
  - frontend/src/pages/RenderPage/AssetsPanel/AssetsPanel.jsx
  - frontend/src/pages/RenderPage/AssetsPanel/CanvasTab.jsx
  - frontend/src/pages/RenderPage/PropertiesPanel/PropertiesPanel.jsx
  - frontend/src/pages/RenderPage/RenderPage.jsx
  - frontend/src/functions/outlineTemplates.js
autonomous: true
requirements: []
must_haves:
  truths:
    - "User sees shapes organized in Primitives and Configure sections"
    - "Polygon is listed first in Primitives section"
    - "Configure section buttons are disabled when fewer than 2 outlines selected"
    - "User can offset an outline by entering a distance value"
    - "Offset result produces a new outline (expanded or contracted)"
  artifacts:
    - path: "frontend/src/pages/RenderPage/AssetsPanel/ShapesTab.jsx"
      provides: "Reorganized shapes menu with 3 sections"
      contains: "Primitives"
    - path: "frontend/src/functions/outlineTemplates.js"
      provides: "Template data structure and parametric generators"
      exports: ["BUILTIN_TEMPLATES", "generateTemplate"]
  key_links:
    - from: "ShapesTab.jsx"
      to: "RenderPage.jsx"
      via: "setPendingPlacement callback"
      pattern: "setPendingPlacement"
    - from: "ShapesTab.jsx"
      to: "outlineTemplates.js"
      via: "template import"
      pattern: "import.*outlineTemplates"
---

<objective>
Reorganize the ShapesTab menu into Primitives (with Polygon first) and Configure sections (boolean ops disabled until multi-select), add parametric template generators and outline templates data, and wire offset/buffer controls into the Toolbar for expanding/contracting outlines.

Purpose: Establishes the core menu structure and template data that all subsequent plans build on. Sets up the offset tool as the first configurable outline operation.
Output: Reorganized ShapesTab, updated AssetsPanel with new tab, new outlineTemplates.js with parametric generators, offset controls in Toolbar.
</objective>

<execution_context>
@/home/user/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/user/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-outline-stage-enhancements-boolean-ops-vertex-editing-templates-import-export-validation-geocoding-snapping-measurements/05.1-RESEARCH.md

@frontend/src/pages/RenderPage/AssetsPanel/ShapesTab.jsx
@frontend/src/pages/RenderPage/AssetsPanel/AssetsPanel.jsx
@frontend/src/pages/RenderPage/Toolbar/Toolbar.jsx
@frontend/src/pages/RenderPage/PropertiesPanel/PropertiesPanel.jsx
@frontend/src/pages/RenderPage/RenderPage.jsx

<interfaces>
<!-- Current ShapesTab interface: -->
export default function OutlinesTab({ setPendingPlacement, activeTool })
// Items: rectangle, circle, triangle(3), pentagon(5), hexagon(6), octagon(8), polygon

<!-- Current Toolbar interface: -->
export default function Toolbar({ selectedShape, updateShape, deleteShape, duplicateShape })
// Currently: rotate left/right, duplicate, delete

<!-- Current AssetsPanel outline mode renders: -->
<ShapesTab setPendingPlacement={setPendingPlacement} activeTool={activeTool} />
<CanvasTab canvasSettings={canvasSettings} setCanvasSettings={setCanvasSettings} mapDistance={mapDistance} setMapDistance={setMapDistance} />

<!-- RenderPage addShape callback: -->
const addShape = useCallback((shapeData) => { ... })
// Accepts: { type, x, y, width, height, points, fill, stroke, strokeWidth, sides, radius, lat, lng, widthMeters, heightMeters }

<!-- Outline shape data shape: -->
{ id, type, name, lat, lng, widthMeters, heightMeters, fill, stroke, strokeWidth, opacity, rotation, sides, radius, radiusMeters, points, pointsGeo, level }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reorganize ShapesTab menu and create outline templates data</name>
  <files>
    frontend/src/pages/RenderPage/AssetsPanel/ShapesTab.jsx,
    frontend/src/functions/outlineTemplates.js
  </files>
  <read_first>
    frontend/src/pages/RenderPage/AssetsTab/ShapesTab.jsx,
    frontend/src/pages/RenderPage/AssetsPanel/SectionsTab.jsx,
    frontend/src/pages/RenderPage/AssetsPanel/AssetsPanel.jsx
  </read_first>
  <action>
Create `frontend/src/functions/outlineTemplates.js` with parametric template generators. Define `BUILTIN_TEMPLATES` array with L-shape, U-shape, T-shape, and Courtyard presets. Each template has: `{ id, name, category, icon, generate: (params) => { points, width, height } }`. The `generate` function returns normalized points array ([[x,y], ...]) for a given parametric size. L-shape generates a 6-point polygon. U-shape generates an 8-point polygon. T-shape generates a 7-point polygon. Courtyard generates a 4-point rectangle with inward notch.

Reorganize `ShapesTab.jsx` into three sections:
1. **Primitives** (h4 heading): Polygon (first item, icon "✎"), Rectangle ("▭"), Circle ("○"), Triangle ("△", sides:3), Pentagon ("⬠", sides:5), Hexagon ("⬡", sides:6), Octagon ("⯃", sides:8)
2. **Templates** (h4 heading): Render BUILTIN_TEMPLATES as clickable items. Each template item calls `setPendingPlacement({ type: "template", templateId: template.id, active: true })` on click.
3. **Configure** (h4 heading): Union ("⊕"), Subtract ("⊖"), Intersect ("⊗"), Offset ("⊞"). All four are rendered with `className` including `tool-item-disabled` when `selectedCount < 2` (passed as new prop). Union/Subtract/Intersect call `onBooleanOp?.(opType)` where opType is "union", "subtract", "intersect". Offset calls `onShowOffset?.()`.

Keep the existing `handleAdd` logic for primitives. The component signature becomes:
```jsx
export default function ShapesTab({ setPendingPlacement, activeTool, selectedCount = 0, onBooleanOp, onShowOffset })
```

Each section uses the same `<div className="menu-tools-section"><h4>...</h4><ul className="tool-list">...</ul></div>` pattern from SectionsTab.jsx.
  </action>
  <verify>
    <automated>cd /home/user/Documents/projects/Home-Tools-2 && node -e "const t = require('./frontend/src/functions/outlineTemplates.js'); console.log(t.BUILTIN_TEMPLATES.length, typeof t.generateTemplate)" 2>&1 || echo "Module check via grep" && grep -c "BUILTIN_TEMPLATES" frontend/src/functions/outlineTemplates.js && grep -c "Primitives" frontend/src/pages/RenderPage/AssetsPanel/ShapesTab.jsx && grep -c "Configure" frontend/src/pages/RenderPage/AssetsPanel/ShapesTab.jsx</automated>
  </verify>
  <acceptance_criteria>
    - `outlineTemplates.js` exports `BUILTIN_TEMPLATES` array with 4 items (L-shape, U-shape, T-shape, Courtyard)
    - `outlineTemplates.js` exports `generateTemplate(id, params)` function that returns `{ points, width, height }`
    - ShapesTab renders "Primitives" section with 7 items, Polygon listed first
    - ShapesTab renders "Templates" section with 4 builtin templates
    - ShapesTab renders "Configure" section with 4 items (Union, Subtract, Intersect, Offset)
    - Configure items have `tool-item-disabled` class when `selectedCount < 2`
    - Polygon click still triggers `setPendingPlacement({ type: "polygon", active: true })`
    - Template items trigger `setPendingPlacement({ type: "template", templateId: ..., active: true })`
  </acceptance_criteria>
  <done>ShapesTab shows 3 organized sections (Primitives with 7 items, Templates with 4 items, Configure with 4 items). Polygon is first. Configure items disabled when selectedCount < 2. outlineTemplates.js exports BUILTIN_TEMPLATES and generateTemplate.</done>
</task>

<task type="auto">
  <name>Task 2: Wire ShapesTab props through AssetsPanel and add offset controls to Toolbar</name>
  <files>
    frontend/src/pages/RenderPage/AssetsPanel/AssetsPanel.jsx,
    frontend/src/pages/RenderPage/Toolbar/Toolbar.jsx,
    frontend/src/pages/RenderPage/RenderPage.jsx
  </files>
  <read_first>
    frontend/src/pages/RenderPage/AssetsPanel/AssetsPanel.jsx,
    frontend/src/pages/RenderPage/Toolbar/Toolbar.jsx,
    frontend/src/pages/RenderPage/RenderPage.jsx
  </read_first>
  <action>
**AssetsPanel.jsx**: In the outline mode branch (the `return` at the bottom), pass new props to ShapesTab:
- `selectedCount={selectedCount}` (new prop added to AssetsPanel signature)
- `onBooleanOp={onBooleanOp}` (new prop)
- `onShowOffset={onShowOffset}` (new prop)

Update the AssetsPanel function signature to accept `selectedCount`, `onBooleanOp`, and `onShowOffset` parameters.

**Toolbar.jsx**: Add offset/buffer controls. When `selectedShape` exists and `showOffset` is true (new prop), render an offset panel below the existing toolbar buttons:
- A numeric input for offset distance in meters (default 0, step 0.1)
- "Expand" and "Contract" buttons that call `onOffset?.(distance)` with positive/negative values
- A separator line before the offset controls
- Use className `render-floating-toolbar` for the container and inline styles matching existing toolbar button styles

Update Toolbar signature:
```jsx
export default function Toolbar({ selectedShape, updateShape, deleteShape, duplicateShape, showOffset, onOffset })
```

**RenderPage.jsx**: Wire the new props:
- Compute `selectedCount` as the number of outlines with `selectedShapeId` set (for now, pass `outlines.length > 0 ? 1 : 0` as a placeholder until Plan 03 adds multi-select — but the prop plumbing must exist)
- Add `onBooleanOp` callback (stub that logs to console for now, Plan 03 implements real logic)
- Add `onShowOffset` state toggle
- Add `onOffset` callback that creates a new outline offset from the selected outline using Turf.js `buffer` (import `@turf/turf` which is already installed)
- Pass `showOffset={showOffsetState}` and `onOffset={handleOffset}` to Toolbar
- Pass `selectedCount`, `onBooleanOp`, `onShowOffset` to AssetsPanel
- Handle the "template" pendingPlacement type: when `pendingPlacement?.type === "template"`, import `generateTemplate` from outlineTemplates.js and call `addShape` with the generated points

For the offset implementation, use Turf.js: create a GeoJSON polygon from the selected outline's points (screen coords), apply `turf.buffer(feature, distance, { units: 'meters' })`, then convert the buffered result back to screen coords and create a new outline via `addShape`.
  </action>
  <verify>
    <automated>cd /home/user/Documents/projects/Home-Tools-2 && grep -c "onBooleanOp" frontend/src/pages/RenderPage/AssetsPanel/AssetsPanel.jsx && grep -c "onOffset" frontend/src/pages/RenderPage/Toolbar/Toolbar.jsx && grep -c "selectedCount" frontend/src/pages/RenderPage/AssetsPanel/AssetsPanel.jsx && grep -c "showOffset" frontend/src/pages/RenderPage/Toolbar/Toolbar.jsx && grep -c "handleOffset" frontend/src/pages/RenderPage/RenderPage.jsx</automated>
  </verify>
  <acceptance_criteria>
    - AssetsPanel passes `selectedCount`, `onBooleanOp`, `onShowOffset` to ShapesTab
    - Toolbar renders offset panel with distance input and Expand/Contract buttons when `showOffset` is true
    - RenderPage computes and passes `selectedCount` to AssetsPanel
    - RenderPage has `handleOffset` callback that uses Turf.js buffer to offset outlines
    - RenderPage handles `pendingPlacement.type === "template"` by calling generateTemplate and addShape
    - Toolbar offset calls `onOffset(positiveDistance)` for Expand and `onOffset(negativeDistance)` for Contract
  </acceptance_criteria>
  <done>AssetsPanel passes new props to ShapesTab. Toolbar shows offset controls with distance input and Expand/Contract buttons. RenderPage wires all callbacks and handles template placement. Offset uses Turf.js buffer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input → Offset distance | Numeric input passed to Turf.js buffer — validate positive numbers |
| Template data → Outline creation | Template points used to create outlines — validate point array format |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5.1-01 | T (Tampering) | Offset input | mitigate | Clamp offset distance to [-100, 100] meters, reject non-numeric values |
| T-5.1-02 | D (DoS) | Boolean op stub | accept | Stub implementation, no real computation yet — Plan 03 adds real ops |
</threat_model>

<verification>
- ShapesTab renders 3 sections with correct item counts
- Polygon is the first item in Primitives section
- Configure items are visually disabled when selectedCount < 2
- Offset creates a new outline when Expand/Contract is clicked
- Template placement generates an outline from builtin template
- No console errors in dev mode
</verification>

<success_criteria>
1. ShapesTab has Primitives (7 items, Polygon first), Templates (4 items), Configure (4 items) sections
2. outlineTemplates.js exports BUILTIN_TEMPLATES and generateTemplate function
3. Toolbar shows offset controls when showOffset is true
4. Offset creates new outline via Turf.js buffer
5. Template placement works end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-outline-stage-enhancements-boolean-ops-vertex-editing-templates-import-export-validation-geocoding-snapping-measurements/05.1-01-SUMMARY.md`
</output>
