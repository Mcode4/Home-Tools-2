---
phase: 06-render-3d
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/src/hooks/useObjectsHistory.js
  - frontend/src/pages/RenderPage/StageBar/StageBar.jsx
autonomous: true
requirements: [RENDER-05]
must_haves:
  truths:
    - "three, @react-three/fiber, @react-three/drei are installed and importable"
    - "Objects stage button in StageBar is enabled and navigable when sections exist"
    - "useObjectsHistory hook provides objects state, undo/redo, localStorage persistence, and API save"
  artifacts:
    - path: "frontend/package.json"
      provides: "Three.js ecosystem dependencies"
      contains: "three"
    - path: "frontend/src/hooks/useObjectsHistory.js"
      provides: "Objects state management with undo/redo and persistence"
      exports: ["default"]
    - path: "frontend/src/pages/RenderPage/StageBar/StageBar.jsx"
      provides: "Stage navigation with Objects enabled"
      contains: "objects"
  key_links:
    - from: "frontend/src/hooks/useObjectsHistory.js"
      to: "frontend/src/hooks/useOutlineHistory.js"
      via: "Pattern mirroring (adapted for objects_data)"
      pattern: "useOutlineHistory"
    - from: "frontend/src/pages/RenderPage/StageBar/StageBar.jsx"
      to: "frontend/src/pages/RenderPage/RenderPage.jsx"
      via: "Stage navigation callback"
      pattern: "setStage"
---

<objective>
Install Three.js ecosystem dependencies, create the useObjectsHistory hook for objects state management (undo/redo, localStorage TTL, API persistence), and enable the Objects stage button in StageBar.

Purpose: Foundation layer — no 3D rendering or catalog work can begin without dependencies installed and state management in place. Enabling the stage button unblocks UI testing.

Output: Updated package.json with three/r3f/drei, new useObjectsHistory hook, StageBar with Objects stage enabled.
</objective>

<execution_context>
@/home/user/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/user/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-render-3d/06-CONTEXT.md
@.planning/phases/06-render-3d/06-RESEARCH.md

@frontend/src/hooks/useOutlineHistory.js
@frontend/src/pages/RenderPage/StageBar/StageBar.jsx
@frontend/src/pages/RenderPage/RenderPage.jsx
@frontend/src/hooks/useRenderStaging.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Three.js ecosystem and create useObjectsHistory hook</name>
  <files>
    frontend/package.json,
    frontend/src/hooks/useObjectsHistory.js
  </files>
  <action>
Per D-11: Install `three`, `@react-three/fiber`, `@react-three/drei` via npm in the frontend directory. Verify installation succeeds without peer dependency conflicts with React 19.

Per D-09 and D-10: Create `useObjectsHistory.js` by adapting the `useOutlineHistory.js` pattern. The hook must:
- Accept `propertyId` parameter
- Maintain `objects` state array (mirrors `outlines` in useOutlineHistory)
- Provide `setObjects` updater that triggers history snapshots via debounced push
- Implement `undo`/`redo` using history stack with historyIndex
- localStorage persistence with 6h TTL using storage key `render_{propertyId}_objects`
- `saveObjects(propertyId)` async function that POSTs to `/api/renders/{propertyId}` with `{ objects_data: objects }` body
- Return: `{ objects, setObjects, history, historyIndex, canUndo, canRedo, loaded, saving, undo, redo, saveObjects }`
- Import `debounce` from `../functions/map` (same as useOutlineHistory)
- Use the same ref pattern (objectsRef, historyIndexRef, pendingHistoryRef, savingRef, isSavingRef) as useOutlineHistory

Do NOT modify useOutlineHistory.js. The new hook is a parallel implementation for objects_data.
  </action>
  <verify>
    <automated>cd frontend && npm ls three @react-three/fiber @react-three/drei 2>&1 | grep -E "three|fiber|drei" && node -e "require('./src/hooks/useObjectsHistory.js')" 2>&1 || echo "IMPORT_CHECK_MANUAL"</automated>
  </verify>
  <done>three, @react-three/fiber, @react-three/drei installed in node_modules. useObjectsHistory.js exists with exported default function accepting propertyId, returning objects/setObjects/undo/redo/saveObjects/canUndo/canRedo.</done>
</task>

<task type="auto">
  <name>Task 2: Enable Objects stage in StageBar</name>
  <files>
    frontend/src/pages/RenderPage/StageBar/StageBar.jsx
  </files>
  <action>
Per D-01: Enable the Objects stage button in StageBar.jsx. Currently line 12 disables both "objects" and "render3d" unconditionally.

Change the `isDisabled` logic so that:
- "objects" stage is enabled when `hasOutlines` is true (outlines exist, meaning user has completed Outline stage)
- "render3d" stage remains disabled for now (will be enabled in a later plan when 3D Render is ready)
- "sections" stage continues to require `hasOutlines`

The updated isDisabled logic should be:
```
const isDisabled = s.key === "render3d" || (s.key === "sections" && !hasOutlines) || (s.key === "objects" && !hasOutlines);
```

Also update the `title` tooltip for the objects button to say "Add outlines first" when disabled, same pattern as sections.

Additionally, in `RenderPage.jsx`, the StageBar component already receives `hasOutlines={outlines.length > 0}` (line 944). No changes needed there.
  </action>
  <verify>
    <automated>cd frontend && grep -n "isDisabled" src/pages/RenderPage/StageBar/StageBar.jsx</automated>
  </verify>
  <done>Objects stage button enabled when hasOutlines is true. render3d remains disabled. StageBar exports correctly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm install → node_modules | Third-party packages installed; verify versions match research |
| localStorage → browser | Objects data persisted client-side; 6h TTL prevents stale data |
| API POST → backend | Objects data sent to server; auth token from localStorage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | T (Tampering) | useObjectsHistory localStorage | accept | localStorage is same-origin; no PII in objects_data |
| T-06-02 | I (Info Disclosure) | API save endpoint | mitigate | Auth token required; already enforced by get_current_user dependency |
</threat_model>

<verification>
- `npm ls three @react-three/fiber @react-three/drei` shows all three installed
- `useObjectsHistory.js` exports default function, imports debounce from functions/map
- StageBar renders Objects button as enabled when hasOutlines=true, disabled when hasOutlines=false
- render3d button remains disabled
</verification>

<success_criteria>
1. npm install three @react-three/fiber @react-three/drei succeeds
2. useObjectsHistory hook created with full undo/redo/localStorage/API save pattern
3. Objects stage button enabled in StageBar when outlines exist
</success_criteria>

<output>
After completion, create `.planning/phases/06-render-3d/06-01-SUMMARY.md`
</output>
