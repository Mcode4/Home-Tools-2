events {}

http {
    server {
        listen 10000 default_server;

        location / {
            proxy_pass http://localhost:3000;
        }

        location /api/ {
            proxy_pass http://127.0.0.1.8000;
        }
    }
}